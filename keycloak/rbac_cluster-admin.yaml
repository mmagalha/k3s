apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: superadmin-binding
subjects:
- kind: User   # ou Group se você autenticar via OIDC
  name: "mmagalha"   # ou o grupo/usuário do IdP (Azure/Keycloak)
  apiGroup: rbac.authorization.k8s.io
roleRef:
  kind: ClusterRole
  name: cluster-admin
  apiGroup: rbac.authorization.k8s.io
